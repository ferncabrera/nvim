-- Deliberately disabled: the minimal custom statusline lives in lua/config/options.lua, incline.nvim
-- shows per-window info. (The previous 215-line config here was dead code behind enabled = false.)
return { "nvim-lualine/lualine.nvim", enabled = false }
